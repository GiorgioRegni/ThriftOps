rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Supabase Postgres is authoritative for domain data. Firestore only stores
    // backend-managed org/member mirror docs used by Firebase Storage rules.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
